# 参与贡献

欢迎一起完善虾客漫。修复一个报错、补充一种模型适配、改进操作提示，或者把难懂的说明写清楚，都是很有价值的贡献。

## 反馈问题

提交 Issue 时，尽量说明：

1. 你正在使用的系统、浏览器和 Node.js 版本。
2. 从哪一步开始操作，点击了什么。
3. 你原本期待看到什么，实际发生了什么。
4. 浏览器控制台或终端里的报错。
5. 问题是否可以稳定复现。

截图和日志请先遮住 API Key、Cookie、私人素材、账号信息和内部地址。不同模型服务的行为差异很大，涉及外部 API 时也请写明服务类型和模型名，但不要附上真实 Key。

## 提交代码

1. Fork 仓库并创建自己的分支。
2. 让一次提交只解决一个清楚的问题。
3. 修改完成后执行本地检查。
4. 在 Pull Request 中说明改了什么、为什么改、怎么验证。

```powershell
npm ci
npm ci --prefix bff
npm run encoding:check
npm run build
npm audit
npm audit --prefix bff
```

修改 BFF 后，还请启动服务并打开 `http://localhost:8030/api/health`。修改界面后，请至少实际打开受影响页面，检查保存、刷新、错误提示和窄屏显示。

## 开发时请注意

- 功能判断以当前源码入口和实际运行结果为准。
- 新增模型服务时，应让使用者自己配置地址、Key 和模型，不要把共享密钥写进前端。
- 不要提交 `.env`、数据库、日志、依赖目录、构建结果、模型文件、语音包或生成媒体。
- 不要随意删除看似旧的迁移字段；它们可能用于打开以前保存的项目。
- 功能或使用方法变化时，请同步更新 README 和相关文档。
- 不要顺手格式化大量无关文件，让评审可以看清真正的修改。

## 文档贡献

文档请尽量站在第一次使用虾客漫的人角度：先说要做什么，再给操作步骤，最后再解释技术原因。能用正常中文说清楚的地方，不需要堆内部模块名和维护记录。
