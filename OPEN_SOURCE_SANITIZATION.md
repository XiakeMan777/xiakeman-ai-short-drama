# 开源脱敏与清理记录

## 母版

- 源快照：`xiakeman-code-pre-video-remix-20260702-104813.zip`
- 快照日期：2026-07-02
- 母版 SHA-256：`1D5678CD770618CCF2B93579156DA499BD6F9473708AA97CAA69730A3BF77B9D`
- 社区版本：`0.8.0-community.1`

母版压缩包保持不变，用于回滚和复核。社区目录不依赖历史交接文档判断功能。

## 脱敏处理

- 删除 15 个前端内嵌 provider Key，内置 key pool 保留空数组。
- 删除二维码、推广/购买链接、affiliate 参数、生产 bucket 标识和一处遗留公网 IP 判断。
- 用空值/本地值重写环境示例和部署配置。
- 社区版固定关闭 Canvas managed credits，不提供充值或支付服务。
- 社区前台不附带共享 Key，也不提供账号模型配置同步；用户 BYOK 只保存在当前浏览器。
- 移除存在已知风险且当前不需要的 `xlsx` 依赖，保留 CSV 导出。

## 文件清理

首次整理已删除旧内部文档、历史测试、依赖目录、构建结果、运行数据库、日志和发布残留。

2026-08-23 再次从真实入口建立引用图：

- 前端以 `src/main.tsx` 为入口；删除 22 个无 import、无动态/字符串引用的孤立文件。
- 删除内容包括未接入的旧 Step 6 配音 UI、旧 Canvas 工具覆盖层、不可达充值弹窗、旧图片模型适配草稿和孤立音频辅助模块。
- BFF 以 `bff/server.js` 为入口；全部 38 个 JavaScript 运行模块仍可达，没有删除后端 handler。
- 保留历史项目迁移、旧字段归一化、SQL migration、Electron、Docker 和 package scripts。

## 功能性修复

- 修复自定义 provider 在设置合并/迁移时被覆盖的问题。
- 修复 BFF worker 重复函数声明导致的启动失败。
- 修复未登录首页对 `/api/auth/me` 和任务列表产生控制台 401 噪音。
- 修复账号模型配置 401 提示乱码。
- BFF 启动脚本现在会在根目录 `.env` 存在时读取它，使环境说明与实际行为一致。
- Vite 配置改用 `loadEnv` 读取根目录 `.env`，开发端口和 BFF 代理目标不再被硬编码默认值覆盖。
- 修复“尚未创建项目”时保存自定义对话/图片/视频/TTS API，刷新页面后配置被初始化为空的问题。
- 修复 Windows `start.bat` 在 BFF 依赖安装成功后仍误报失败的问题。
- 用交流群二维码替换登录入口，并移除前端会话恢复、账号设置同步、云同步挂载和云项目操作入口。
- 按维护者要求保留公开的 Google Analytics 使用量统计；删除百度统计和失效的外部 favicon 路径。统计脚本不得发送模型 Key 或项目内容。
- 恢复正式版品牌 Logo，并将完整品牌资源纳入 `public/brand`。

## GitHub 发布排除项

- 不发布前端或 BFF 的 `node_modules`、构建结果、桌面安装包、压缩发布包和本地缓存。
- 不发布语音语料库、语音包、本地模型、模型权重、检查点和生成媒体。
- 不发布运行数据库、日志、用户项目、环境变量和任何本地凭据。

## 发布前必须复核

- 敏感信息与内部基础设施扫描
- UTF-8/乱码检查
- TypeScript + Vite 生产构建
- 全部 BFF JavaScript 语法检查和本地健康检查
- 新浏览器上下文冒烟测试
- 根目录和 BFF 依赖审计
- 删除验证生成的 `node_modules`、`dist`、运行数据和浏览器缓存

自动扫描不能证明敏感值从未存在。曾经有效的凭据必须在服务商侧轮换。

## 2026-08-23 验证结果

- UTF-8 检查通过。
- TypeScript 与 Vite 生产构建通过。
- 38 个 BFF JavaScript 运行文件语法检查通过，BFF 本地启动和健康接口通过。
- 前端与 BFF 两套依赖审计均为 0 个已知漏洞。
- 浏览器完成首页、API 设置和空项目配置刷新持久化冒烟测试。
- ESLint 仍有 273 个 error、93 个 warning，已作为继承技术债记录在 `docs/KNOWN_LIMITATIONS.md`，不是本次清理制造的新构建错误。
