# 安全策略

## 密钥和敏感数据

- 不提交真实 API Key、Cookie、Agent Key、数据库 URL、对象存储凭据、证书或用户数据。
- `.env` 已忽略，`.env.example` 只能放占位值和安全的本地默认值。
- 浏览器 BYOK 仅适合个人或可信设备；社区前台没有登录后的服务端配置同步。
- 服务端模型/存储密钥使用 AES-256-GCM。生产环境必须持久化 `APP_SETTINGS_SECRET`、`SETTINGS_ENCRYPTION_KEY` 或 `AUTH_SECRET`，不得每次启动随机更换。

## 网络与 SSRF

- 生产环境设置 LLM、图片、TTS、音乐和 Agent 上游主机白名单。
- 不允许用户把模型地址指向 localhost、云元数据地址或内网管理接口。
- 图片/视频代理只应访问受控域名；反向代理还应限制请求速率和响应大小。
- 浏览器直连 Key 会发往用户填写的地址，保存前必须确认域名拼写。

## 保留接口与权限

- 社区前台不提供注册、登录、管理员或 Agent Key 入口。
- 首页保留 Google Analytics 页面访问统计。它用于匿名使用量分析，不应写入或上报项目内容、模型配置和 API Key；如需完全离线部署，可自行从 `index.html` 移除对应脚本。
- BFF 仍保留这些兼容路由；公网部署应关闭注册，并在反向代理层限制 `/api/auth`、`/api/admin`、`/api/cloud`、`/api/jobs` 和 `/api/agent`。
- 二次接入 Cookie 会话时必须设置 `AUTH_COOKIE_SECURE=true`、启用 HTTPS，并把 `CORS_ORIGIN` 限制为确切来源。
- `/api/render`、公共媒体/代理和 `/api/xyq-agent` 当前没有统一用户认证，必须由反向代理限制，或在二次开发中补上认证。

## 数据和备份

- 持久化数据库、对象存储、`/data` 卷和加密密钥必须一起备份。
- 日志不得记录 Authorization、Cookie、完整 Key、原始密码或私有素材 URL。
- 本地项目应定期导出项目或工作流快照；浏览器数据清理可能删除未导出的项目。

## 发布检查

1. 扫描疑似 Key、私钥、Cookie、真实 IP、内部域名和个人链接。
2. 从干净依赖安装执行编码检查和生产构建。
3. 对全部 BFF JavaScript 执行语法检查并验证 `/api/health`。
4. 执行根目录和 BFF 的依赖审计。
5. 确认没有 `.env`、数据库、日志、媒体、依赖目录和构建产物。
6. 使用新浏览器上下文确认没有登录入口或认证请求，并检查交流群、API 设置和本地项目入口。

## 历史快照提醒

原始快照曾包含前端内嵌 provider Key。社区目录已删除这些值，但从代码中删除并不会让旧 Key 自动失效；任何曾经有效的 Key 都应在服务商侧吊销或轮换。

## 报告问题

不要在公开 Issue 中粘贴可用凭据、用户素材或个人信息。私下联系维护者，提供受影响文件、路由、复现条件和脱敏后的错误信息。
